// const balance = await getBalance();
// const mm200 = await getMovingAverage(200);
// const mm10 = await getMovingAverage(10);

// const priceBAND = await getPriceSymbol(SYMBOL);
// getPriceBaseClose();
// res.end(JSON.stringify({ balance, mm200, mm10, priceBAND }));
